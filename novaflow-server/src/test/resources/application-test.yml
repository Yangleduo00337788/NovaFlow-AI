spring:
  config:
    activate:
      on-profile: test

  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false

liteflow:
  enable: true
  print-execution-log: false

logging:
  level:
    ai.novaflow: info
