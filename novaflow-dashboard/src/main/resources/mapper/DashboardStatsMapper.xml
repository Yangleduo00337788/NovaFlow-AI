<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ai.novaflow.dashboard.mapper.DashboardStatsMapper">

    <select id="countApplications" resultType="java.lang.Long">
        SELECT COUNT(*) FROM application WHERE tenant_id = #{tenantId} AND is_deleted = 0
    </select>

    <select id="countAgents" resultType="java.lang.Long">
        SELECT COUNT(*) FROM agent WHERE tenant_id = #{tenantId} AND is_deleted = 0
    </select>

    <select id="countKnowledgeBases" resultType="java.lang.Long">
        SELECT COUNT(*) FROM knowledge_base WHERE tenant_id = #{tenantId} AND is_deleted = 0
    </select>

    <select id="recentUsageLogs" resultType="ai.novaflow.dashboard.domain.RecentUsageLogRow">
        SELECT tu.id,
               COALESCE(a.agent_name, '未知 Agent') AS agent_name,
               tu.total_tokens,
               tu.latency_ms,
               tu.created_at,
               tu.success,
               tu.trace_id
        FROM token_usage tu
        LEFT JOIN agent a ON tu.agent_id = a.id
        WHERE tu.tenant_id = #{tenantId}
        ORDER BY tu.created_at DESC
        LIMIT #{limit}
    </select>

    <select id="topApplications" resultType="ai.novaflow.dashboard.domain.NamedCountRow">
        SELECT COALESCE(app.app_name, '未命名应用') AS name,
               COUNT(*) AS value
        FROM token_usage tu
        LEFT JOIN application app ON tu.application_id = app.id
        WHERE tu.tenant_id = #{tenantId}
          AND tu.usage_date &gt;= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY tu.application_id, app.app_name
        ORDER BY value DESC
        LIMIT 5
    </select>

    <select id="latestPublishedWorkflow" resultType="ai.novaflow.dashboard.domain.WorkflowRuntimeRow">
        SELECT NULL AS execution_id,
               w.id AS workflow_id,
               w.status,
               w.workflow_name
        FROM workflow w
        WHERE w.tenant_id = #{tenantId}
          AND w.is_deleted = 0
          AND w.status = 1
        ORDER BY w.created_at DESC
        LIMIT 1
    </select>

    <select id="listPublishedWorkflows" resultType="ai.novaflow.dashboard.domain.PublishedWorkflowRow">
        SELECT w.id AS workflow_id,
               w.workflow_name,
               w.status,
               COALESCE(app.app_name, '未命名应用') AS application_name,
               w.updated_at
        FROM workflow w
        LEFT JOIN application app ON w.application_id = app.id AND app.is_deleted = 0
        WHERE w.tenant_id = #{tenantId}
          AND w.is_deleted = 0
          AND w.status = 1
        ORDER BY w.created_at DESC
        LIMIT #{limit}
    </select>

    <select id="latestExecutionForWorkflow" resultType="ai.novaflow.dashboard.domain.WorkflowRuntimeRow">
        SELECT we.execution_id,
               we.workflow_id,
               we.status,
               w.workflow_name
        FROM workflow_execution we
        INNER JOIN workflow w ON we.workflow_id = w.id AND w.is_deleted = 0
        WHERE we.tenant_id = #{tenantId}
          AND we.workflow_id = #{workflowId}
        ORDER BY we.started_at DESC
        LIMIT 1
    </select>

    <select id="findWorkflow" resultType="ai.novaflow.dashboard.domain.WorkflowRuntimeRow">
        SELECT w.id AS workflow_id,
               w.workflow_name,
               w.status,
               NULL AS execution_id
        FROM workflow w
        WHERE w.tenant_id = #{tenantId}
          AND w.id = #{workflowId}
          AND w.is_deleted = 0
        LIMIT 1
    </select>

    <select id="workflowNodes" resultType="ai.novaflow.dashboard.domain.WorkflowNodeRow">
        SELECT node_id, node_name, node_type, sort_order, position_x, position_y
        FROM workflow_node
        WHERE tenant_id = #{tenantId}
          AND workflow_id = #{workflowId}
        ORDER BY sort_order, id
    </select>

    <select id="workflowCanvasData" resultType="java.lang.String">
        SELECT canvas_data
        FROM workflow
        WHERE id = #{workflowId}
          AND tenant_id = #{tenantId}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="workflowEdges" resultType="ai.novaflow.dashboard.domain.WorkflowEdgeRow">
        SELECT edge_id, source_node_id, target_node_id, source_handle, target_handle, `condition` AS condition_expr
        FROM workflow_edge
        WHERE tenant_id = #{tenantId}
          AND workflow_id = #{workflowId}
        ORDER BY id
    </select>

    <select id="workflowNodeLogs" resultType="ai.novaflow.dashboard.domain.WorkflowNodeLogRow">
        SELECT node_id, status
        FROM workflow_node_log
        WHERE execution_id = #{executionId}
    </select>

    <select id="hourlyTrend" resultType="ai.novaflow.dashboard.domain.TrendPointRow">
        SELECT CONCAT(LPAD(HOUR(MIN(created_at)), 2, '0'), ':00') AS time_label,
               COUNT(*) AS value
        FROM token_usage
        WHERE tenant_id = #{tenantId}
          AND created_at &gt;= DATE_SUB(NOW(), INTERVAL 24 HOUR)
        GROUP BY HOUR(created_at)
        ORDER BY HOUR(created_at)
    </select>

    <select id="dailyCostSparkline" resultType="ai.novaflow.dashboard.domain.DailySparklineRow">
        SELECT DATE_FORMAT(usage_date, '%m-%d') AS day_label,
               CAST(ROUND(COALESCE(SUM(cost), 0) * 100) AS SIGNED) AS value
        FROM token_usage
        WHERE tenant_id = #{tenantId} AND currency = #{currency}
          AND usage_date &gt;= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY usage_date
        ORDER BY usage_date
    </select>

    <select id="dailyInvocationSparkline" resultType="ai.novaflow.dashboard.domain.DailySparklineRow">
        SELECT DATE_FORMAT(usage_date, '%m-%d') AS day_label,
               COUNT(*) AS value
        FROM token_usage
        WHERE tenant_id = #{tenantId}
          AND usage_date &gt;= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY usage_date
        ORDER BY usage_date
    </select>

    <select id="dailyTokenSparkline" resultType="ai.novaflow.dashboard.domain.DailySparklineRow">
        SELECT DATE_FORMAT(usage_date, '%m-%d') AS day_label,
               COALESCE(SUM(total_tokens), 0) AS value
        FROM token_usage
        WHERE tenant_id = #{tenantId}
          AND usage_date &gt;= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY usage_date
        ORDER BY usage_date
    </select>

    <select id="countApplicationsBeforeDays" resultType="java.lang.Long">
        SELECT COUNT(*) FROM application
        WHERE tenant_id = #{tenantId} AND is_deleted = 0
          AND created_at &lt; DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
    </select>

    <select id="countAgentsBeforeDays" resultType="java.lang.Long">
        SELECT COUNT(*) FROM agent
        WHERE tenant_id = #{tenantId} AND is_deleted = 0
          AND created_at &lt; DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
    </select>

    <select id="countKnowledgeBasesBeforeDays" resultType="java.lang.Long">
        SELECT COUNT(*) FROM knowledge_base
        WHERE tenant_id = #{tenantId} AND is_deleted = 0
          AND created_at &lt; DATE_SUB(CURDATE(), INTERVAL #{days} DAY)
    </select>

    <select id="dailyApplicationSparkline" resultType="ai.novaflow.dashboard.domain.DailySparklineRow">
        SELECT DATE(MIN(created_at)) AS stat_date,
               DATE_FORMAT(MIN(created_at), '%m-%d') AS day_label,
               COUNT(*) AS value
        FROM application
        WHERE tenant_id = #{tenantId} AND is_deleted = 0
          AND created_at &gt;= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY DATE(created_at)
        ORDER BY DATE(created_at)
    </select>

    <select id="dailyAgentSparkline" resultType="ai.novaflow.dashboard.domain.DailySparklineRow">
        SELECT DATE(MIN(created_at)) AS stat_date,
               DATE_FORMAT(MIN(created_at), '%m-%d') AS day_label,
               COUNT(*) AS value
        FROM agent
        WHERE tenant_id = #{tenantId} AND is_deleted = 0
          AND created_at &gt;= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY DATE(created_at)
        ORDER BY DATE(created_at)
    </select>

    <select id="dailyKnowledgeSparkline" resultType="ai.novaflow.dashboard.domain.DailySparklineRow">
        SELECT DATE(MIN(created_at)) AS stat_date,
               DATE_FORMAT(MIN(created_at), '%m-%d') AS day_label,
               COUNT(*) AS value
        FROM knowledge_base
        WHERE tenant_id = #{tenantId} AND is_deleted = 0
          AND created_at &gt;= DATE_SUB(CURDATE(), INTERVAL 6 DAY)
        GROUP BY DATE(created_at)
        ORDER BY DATE(created_at)
    </select>

</mapper>
